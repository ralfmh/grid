/*
** The default material for a block, in case none is defined
** at the requested position.
*/
const string DEFAULT_MATERIAL = "air";

/*
** The default text, in case none is defined
** at the requested position, or the position lies outside the map boundaries.
*/
const string DEFAULT_TEXT = "";

/*
** Segments.
*/
const string SEGMENT_FX = "fx";
const string SEGMENT_FY = "fy";
const string SEGMENT_LV = "lv";
const string SEGMENT_ML = "ml";
const string SEGMENT_PA = "pa";
const string SEGMENT_PN = "pn";
const string SEGMENT_PS = "ps";
const string SEGMENT_PT = "pt";
const string SEGMENT_PX = "px";
const string SEGMENT_TX = "tx";
const string SEGMENT_TY = "ty";

/*
** Choices (available segment values).
*/
const string CHOICE_M = "m";
const string CHOICE_U = "u";

/*
** Mapping of choices to segment.
** [0]: Segment.
** [1]: Choice 1.
** [2]: Choice n...
*/
const string[][] SEGMENT_CHOICE =
{
  {
    SEGMENT_PA,
    CHOICE_M,
    CHOICE_U
  }
};

/*
** Defines segments whose values are digits.
*/
const string[] DIGIT_SEGMENT =
{
  SEGMENT_FX,
  SEGMENT_FY,
  SEGMENT_PT,
  SEGMENT_TX,
  SEGMENT_TY
};

/*
** Entity types.
*/
const string TYPE_LADDER = "ladr";
const string TYPE_PORTAL = "port";
const string TYPE_TEXT = "text";
const string TYPE_TILE = "tile";
const string TYPE_WALL = "wall";

string[] ENTITY_TYPE =
{
  TYPE_LADDER,
  "none",
  TYPE_PORTAL,
  TYPE_TEXT,
  TYPE_TILE,
  TYPE_WALL
};

string[] ENTITY_TYPE_NAME =
{
  "ladder",
  "none",
  "portal",
  "text",
  "tile",
  "wall"
};

/*
** Map definition array.
** [0]: Entity type.
** [1]: Segment 1.
** [2]: Segment n...
*/
const string[][] MAP_DEFINITION =
{
  {
    TYPE_LADDER,
    SEGMENT_FX,
    SEGMENT_FY,
    SEGMENT_ML,
    SEGMENT_TX,
    SEGMENT_TY
  },
  {
    TYPE_PORTAL,
    SEGMENT_FX,
    SEGMENT_FY,
    SEGMENT_PA,
    SEGMENT_PN,
    SEGMENT_PS,
    SEGMENT_PT,
    SEGMENT_PX,
    SEGMENT_TX,
    SEGMENT_TY
  },
  {
    TYPE_TEXT,
    SEGMENT_FX,
    SEGMENT_FY,
    SEGMENT_LV,
    SEGMENT_TX,
    SEGMENT_TY
  },
  {
    TYPE_TILE,
    SEGMENT_FX,
    SEGMENT_FY,
    SEGMENT_ML,
    SEGMENT_TX
  },
  {
    TYPE_WALL,
    SEGMENT_FX,
    SEGMENT_FY,
    SEGMENT_ML,
    SEGMENT_TX,
    SEGMENT_TY
  }
};

/*
** The minimum x position of each map.
** This position cannot be undercut due to the current implementation.
*/
const int MAP_MIN_X = 0;

/*
** The minimum x position of each map.
** This position cannot be undercut due to the current implementation.
*/
const int MAP_MIN_Y = 0;

/*
** The character which separates lines in the definition.
*/
const string LINE_SEPARATOR = "\n";

/*
** The character which separates segments in a line.
*/
const string SEGMENT_SEPARATOR = " ";

/*
** Returns an array of required segments for a given type.
*/
string[] GetRequiredSegments(
  int typeIndex // The index of the type.
)
{
  string[] segments;
  for (int i = 1; i < MAP_DEFINITION[typeIndex].length(); i++)
  {
    segments.insert_last(MAP_DEFINITION[typeIndex][i]);
  }
  return segments;
}

/*
** Searches the given 2-dimensional array for a value in the inner array.
** Returns the 0-based index of the outer array, or -1 if the value was not found.
*/
int Find2D(
  string[][] array, // The Array to search.
  int innerIndex, // The index of the inner array.
  string value  // The value to search for.
)
{
  for (int i = 0; i < array.length(); i++)
  {
    if (array[i][0] == value)
    {
      return i;
    }
  }
  return -1;
}