#include "../../src/map/map.nvgt"

void test_person_view_portal()
{
  string definition = "tile fx0 tx10 fy0 mlstone" + LINE_SEPARATOR
                      + "port fx3 fy0 tx5 ty0 pam pt0";
  Map map;
  string[] errors = map.Parse(definition);
  assert(errors.length() == 0, join(errors, ", "));
  map.CreateGrids();
  Person p(0, 0, map);
  assert(p.View(Right) == "portal (3), ", p.View(Right));
}