#include "../../src/map/map.nvgt"

void test_block_ladder()
{
  string definition = "ladr fx0 tx0 fy0 ty10 mlmetal";
  Map map;
  map.Parse(definition);
  map.CreateGrids();
  assert(map.GetBlock(0, 5) == "metal");
}