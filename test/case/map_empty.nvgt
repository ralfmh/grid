#include "../../src/map/map.nvgt"

void test_map_empty()
{
  Map map;
  string[] errors = map.Parse("");
  assert(errors.length() == 1);
  assert(string_contains(errors[0], "empty", 1) > -1);
}